/**
 * Application-facing Pure Reactive Protocol Java API.
 *
 * <p>The native protocol core is intentionally independent of Spring, Reactor, JSON and the Alpha Lab.
 * Optional integrations live in explicit subpackages so this package root can later be extracted as a
 * standalone Java SDK without moving {@code dev.prplab.*} application code.</p>
 */
package com.byeolnaerim.prp;
